agent:
  name: research-agent
  type: ai-agent
  role: research-agent

  credentials:
    - OPENAI_API_KEY

  tools:
    - name: web-search
      permissions:
        - search:read

    - name: document-reader
      permissions:
        - document:read